CREATE TABLE `FILE` (
	`FILE_IDX` INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`FILE_NAME` VARCHAR NOT NULL COMMENT '암호화여부?',
	`FILE_DIRECTORY` VARCHAR NOT NULL,
	`FILE_TYPE` CHAR NOT NULL COMMENT '사용 타입 정의해서 넣기',
	`FILE_CREATE_DATE` DATETIME NOT NULL,
	`FILE_DELETE_DATE` DATETIME NULL,
	`FILE_DELETE_YN`	boolean NOT NULL,
	`BOARD_IDX` INT NOT NULL,
	`AD_IDX` INT NOT NULL,
	`QNA_IDX` INT NOT NULL,
    FOREIGN KEY ( BOARD_IDX ) REFERENCES BOARD(BOARD_IDX),
    FOREIGN KEY ( AD_IDX ) REFERENCES AD(AD_IDX),
    FOREIGN KEY ( QNA_IDX ) REFERENCES QNA(QNA_IDX)
);
CREATE TABLE `USERS` (
	`USER_IDX` INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`USER_ID` VARCHAR NOT NULL,
	`USER_PW` VARCHAR NULL,
	`USER_NICKNAME` VARCHAR NULL,
	`CREATE_DATE` DATETIME NULL,
	`UPDATE_DATE` DATETIME NULL,
	`DELETE_DATE` DATETIME NULL,
	`USER_POLICY_YN` CHAR NULL,
	`AUTHORITY_IDX` INT NOT NULL,
    FOREIGN KEY ( AUTHORITY_IDX ) REFERENCES AUTHORITY(AUTHORITY_IDX)
);
CREATE TABLE `BOARD` (
	`BOARD_IDX` INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`BOARD_TITLE` VARCHAR NOT NULL,
	`BOARD_TYPE` CHAR NOT NULL COMMENT '타입정의 필요',
	`BOARD_GOOD_COUNT` INT NOT NULL,
	`BOARD_CREATE_DATE` DATETIME NOT NULL,
	`BOARD_DELETE_DATE` DATETIME NULL,
	`BOARD_UPDATE_DATE` DATETIME NULL,
	`BOARD_READ_COUNT` INT NOT NULL,
	`BOARD_BAD_COUNT` INT NOT NULL,
	`BOARD_CONTENT` TEXT NOT NULL COMMENT '태그 포함 저장?',
	`USER_IDX` INT NOT NULL,
    FOREIGN KEY ( USER_IDX ) REFERENCES USER(USER_IDX)
);
CREATE TABLE `COMMENT` (
	`COMMENT_IDX` INT NULL AUTO_INCREMENT PRIMARY KEY COMMENT '계층형',
	`COMMENT_PARENT_IDX` VARCHAR(255) NULL COMMENT 'Field` VARCHAR(255) NUL',
	`CREATE_DATE`	DATETIME	NULL,
	`UPDATE_DATE`	DATETIME	NULL,
	`DELETE_DATE`	DATETIME	NULL,
	`BOARD_IDX`	INT	NOT NULL,
    FOREIGN KEY ( BOARD_IDX ) REFERENCES BOARD(BOARD_IDX)
);

CREATE TABLE `DECLARATION` (
	`DECLARATION_IDX`	INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`DECLARATION_TARGET_IDX`	INT	NULL,
	`DECLARATION_TARGET_TYPE`	CHAR	NULL,
	`CREATE_DATE`	DATETIME	NULL,
	`UPDATE_DATE`	DATETIME	NULL,
	`DELETE_DATE`	DATETIME	NULL
);

CREATE TABLE `POLICY` (
	`POLICY_IDX`	INT	NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`POLICY_CONTENT`	TEXT	NULL,
	`POLICY_TITLE`	VARCHAR	NULL,
	`POLICY_TYPE`	CHAR	NULL,
	`CREATE_DATE`	DATETIME	NULL,
	`UPDATE_DATE`	DATETIME	NULL,
	`DELETE_DATE`	DATETIME	NULL
);

CREATE TABLE `FAQ` (
	`FAQ_IDX`	INT	NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`FAQ_TITLE`	VARCHAR	NULL,
	`FAQ_CONTENT`	TEXT	NULL,
	`CREATE_DATE`	DATETIME	NULL,
	`UPDATE_DATE`	DATETIME	NULL,
	`DELETE_DATE`	DATETIME	NULL,
	`USER_IDX`	INT	NOT NULL,
    FOREIGN KEY ( USER_IDX ) REFERENCES USER(USER_IDX)
);

CREATE TABLE `AD` (
	`AD_IDX`	INT	NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`AD_TITLE`	VARCHAR	NULL,
	`AD_START_DATE`	DATETIME	NULL,
	`AD_END_DATE`	DATETIME	NULL,
	`CREATE_DATE`	DATETIME	NULL,
	`UPDATE_DATE`	DATETIME	NULL,
	`DELETE_DATE`	DATETIME	NULL,
	`AD_LINK`	VARCHAR	NULL,
	`USER_IDX`	INT	NOT NULL,
    FOREIGN KEY ( USER_IDX ) REFERENCES USER(USER_IDX)
);

CREATE TABLE `ANSWER` (
	`ANSWER_IDX`	INT	NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`ANSWER_CONTENT`	TEXT	NULL,
	`CREATE_DATE`	DATETIME	NULL,
	`UPDATE_DATE`	DATETIME	NULL,
	`DELETE_DATE`	DATETIME	NULL,
	`QNA_IDX`	INT	NOT NULL,
	`USER_IDX`	INT	NOT NULL,
    FOREIGN KEY ( QNA_IDX ) REFERENCES USER(USER_IDX),
    FOREIGN KEY ( USER_IDX ) REFERENCES USER(USER_IDX)
);

CREATE TABLE `QNA` (
	`QNA_IDX`	INT	NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`QNA_TITLE`	VARCHAR	NULL,
	`QNA_CONTENT`	TEXT	NULL,
	`CREATE_DATE`	DATETIME	NULL,
	`UPDATE_DATE`	DATETIME	NULL,
	`DELETE_DATE`	DATETIME	NULL,
	`USER_IDX`	INT	NOT NULL,
    FOREIGN KEY ( USER_IDX ) REFERENCES USER(USER_IDX)
);

CREATE TABLE `RECOMMENDED_LOG` (
	`RECOMMENDED_LOG_IDX`	INT	NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`USER_IDX`	INT	NULL,
	` TYPE`	CHAR	NULL,
	`CREATE_DATE`	DATETIME	NULL,
	`UPDATE_DATE`	DATETIME	NULL,
	`DELETE_DATE`	DATETIME	NULL,
	`BOARD_IDX`	INT	NOT NULL,
    FOREIGN KEY ( BOARD_IDX ) REFERENCES BOARD(BOARD_IDX)
);

CREATE TABLE `AUTHORITY` (
	`AUTHORITY_IDX`	INT	NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`AUTHORITY_GRANT`	CHAR	NULL,
	`CREATE_DATE`	DATETIME	NULL,
	`UPDATE_DATE`	DATETIME	NULL,
	`DELETE_DATE`	DATETIME	NULL
);

CREATE TABLE `WARD` (
	`WARD_IDX`	INT	NOT NULL AUTO_INCREMENT PRIMARY KEY,
	` FIELD`	VARCHAR(255)	NULL,
	`BOARD_IDX`	INT	NOT NULL,
	`USER_IDX`	INT	NOT NULL,
    FOREIGN KEY ( BOARD_IDX ) REFERENCES BOARD(BOARD_IDX),
    FOREIGN KEY ( USER_IDX ) REFERENCES USER(USER_IDX)
);

CREATE TABLE `USER_LEVEL` (
	`USER_LEVEL_IDX`	INT	NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`USER_LEVEL_EXP`	INT	NULL,
	`CREATE_DATE`	DATETIME	NULL,
	`UPDATE_DATE`	DATETIME	NULL,
	`DELETE_DATE`	DATETIME	NULL,
	`LEVEL_IDX`	INT	NULL,
	`USER_IDX`	INT	NOT NULL,
    FOREIGN KEY ( LEVEL_IDX ) REFERENCES LEVEL(LEVEL_IDX),
    FOREIGN KEY ( USER_IDX ) REFERENCES USER(USER_IDX)
);

CREATE TABLE `LEVEL_POLICY` (
	`LEVEL_POLICY_IDX`	INT	NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`LEVEL_POLICY_TYPE`	CHAR	NULL,
	`LEVEL_POLICY_EXP`	INT	NULL,
	`CREATE_DATE`	DATETIME	NULL,
	`UPDATE_DATE`	DATETIME	NULL,
	`DELETE_DATE`	DATETIME	NULL
);

CREATE TABLE `USER_POLICY` (
	`USER_POLICY_IDX`	INT	NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`CREATE_DATE`	DATETIME	NULL,
	`UPDATE_DATE`	DATETIME	NULL,
	`DELETE_DATE`	DATETIME	NULL,
	`USER_IDX`	INT	NOT NULL,
	`POLICY_IDX`	INT	NOT NULL,
    FOREIGN KEY ( USER_IDX ) REFERENCES USER(USER_IDX),
    FOREIGN KEY ( POLICY_IDX ) REFERENCES POLICY(POLICY_IDX)
);

CREATE TABLE ` LEVEL` (
	`LEVEL_IDX`	INT	NOT NULL AUTO_INCREMENT PRIMARY KEY,
	`LEVEL_GRADE`	INT	NULL,
	`LEVEL_EXP`	INT	NULL,
	`CREATE_DATE`	DATETIME	NULL,
	`UPDATE_DATE`	DATETIME	NULL,
	`DELETE_DATE`	DATETIME	NULL
);


